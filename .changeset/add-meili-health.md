---
"@meilisearch/cli": minor
---

Add `meili health`, the first command, on top of the CLI skeleton: global flags, `json`/`ndjson`/`table` output modes, structured errors with stable exit codes, connection resolution from flags, environment variables and contexts, and `--debug` HTTP traces with secrets redacted.
