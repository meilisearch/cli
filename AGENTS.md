# Agent instructions

The maintainer guide for this repository is `CLAUDE.md`. It applies to every coding agent, not only Claude.

Read `CLAUDE.md` fully before you change anything. Then read the contract that applies to your task in `docs/contract/`.
